[project]
name = "triage-keepers-proto"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mediapipe>=0.10.14",
    "opencv-python-headless>=4.9.0",
    "Pillow>=10.3.0",
    "mcp>=1.0.0",
    "pytest>=8.0",
]
