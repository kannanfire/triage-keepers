# TRIAGE_MAX_WORKERS
# Number of worker threads for concurrent CV scoring in index_folder.
# Default: 1 (HDD-safe, avoids contention on mechanical drives)
# SSD: set to 4-8 for faster indexing
# HDD: keep at 1 or 2 to avoid excessive disk thrashing
TRIAGE_MAX_WORKERS=1
